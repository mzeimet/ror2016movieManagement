<div class="container">
<p id="notice"><%= notice %></p>

<h1>Platform <%= @pname %></h1>

<table class ="table table-condensed table-bordered">
  <thead>
    <tr>
      <th>Video Name</th>
      <th>Borrowed</th>
      <th>Borrowed until</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @platforms.each do |platform| %>
      <tr>
        <td>
          <% if !platform.video.nil? %>
            <%= platform.video.name %>
          <% end %>
        </td>
        <td><% if platform.borrowed %>
                <span class="glyphicon glyphicon-ok" aria-hidden="true"></span>
              <%else %>
                <span class="glyphicon glyphicon-remove" aria-hidden="true"></span>
              <% end %></td>
        <td><%= convert_date(platform.borrowedDate) %></td>
        <td><%= link_to '<span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span>'.html_safe, platform %></td>
        <td><%= link_to '<span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>'.html_safe, edit_platform_path(platform) %> </td>
        <td><%= link_to '<span class="glyphicon glyphicon-trash" aria-hidden="true"></span>'.html_safe, platform, method: :delete, data: { confirm: 'Are you sure?' } %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Back', platforms_path %>
</div>
