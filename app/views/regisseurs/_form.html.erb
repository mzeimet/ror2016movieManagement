<%= bootstrap_form_for(@regisseur) do |f| %>
  <% if @regisseur.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@regisseur.errors.count, "error") %> prohibited this regisseur from being saved:</h2>

      <ul>
      <% @regisseur.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.file_field :cover %>
  </div><br>
  <div class="actions">
    <%= f.submit %>
  </div><br><br>
<% end %>
