<%= bootstrap_form_for(@regisseur) do |f| %>
<% if @regisseur.errors.any? %>
<div id="error_explanation">
  <div class="alert alert-danger" role="alert">
    <p id="notice">  <h4><%= pluralize(@regisseur.errors.count, "error") %> prohibited this regisseur from being saved</h4>
    </p>
  </div>
</div>
<% end %>

<div class="field">
  <%= f.text_field :name %>
</div>
<div class="field">
  <%= f.file_field :cover %>
</div><br>
<div class="actions">
  <%= f.submit %>
</div><br><br>
<% end %>
