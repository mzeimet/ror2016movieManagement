<%= bootstrap_form_for(@season) do |f| %>
  <% if @season.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@season.errors.count, "error") %> prohibited this season from being saved:</h2>

      <ul>
      <% @season.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <h4>Series: <%=@season.series.name%></h4>
    <%= f.hidden_field :series, :value => @season.series.id%>
  </div>
  <div class="field">
    <%= f.number_field :number ,label: "Season number", in: 1...100%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div><br><br>
<% end %>
